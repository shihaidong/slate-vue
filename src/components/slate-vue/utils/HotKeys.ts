// hotkey中的函数返回true则表示不执行接下来的操作

export const HotKeys = {
  Arrow: (e: KeyboardEvent) => {
    e.preventDefault()
  }
}

function registerHotKey() {
  
}

export const useRegisterHotKey = () => {
  
}